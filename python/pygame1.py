import pygame
import sys
screen=pygame.display.set_mode(size=(640,480))
screen.fill(255,255,255)

run=True
j;alksjdf;lksjad;fkj
while run:
    for event in pygame.get():
        if event.type==pygame.QUIT:
            run=False


pygame.quit()

